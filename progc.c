#include "stdio.h"
#include "stdlib.h"

int main(int argc, char *argv[])
{
    if (argc < 2) {
        return 1;
    }
    
    int file = fopen(argv[1]);
    seekf(file, -1);
    int end = ...

    char ligne[100];

    while (file fini) {
        ligne = lirelignedufichier
        
        parse la ligne
        
        printf("wget %s", lechemin);
    }
    //
    
    return 0;
}

